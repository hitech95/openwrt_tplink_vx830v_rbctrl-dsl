[package]
name = "af_packet"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "AF_PACKET raw Ethernet socket for Linux (libc-only, no external C deps)"
publish = false

[dependencies]
libc = { workspace = true }
